# Copy to `.codex/env.allowlist` only if you need extra non-secret variables
# inherited into Codex sessions launched through `.codex/bin/codex-safe`.
#
# One env var name per line. Secret-like names are ignored by the launcher.
# Examples of relatively safe entries:
#
# CI
# NEXT_PUBLIC_APP_ENV
# NODE_OPTIONS
